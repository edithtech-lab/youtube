# 유튜브 쇼츠/롱폼 자동 제작 파이프라인

한국어 정보성 다큐 톤 채널의 제작 도구. `main.py` 한 파일에 백엔드 전부(약 8,600줄),
`ui/index.html` 한 파일에 프론트 전부. PySide6 QWebChannel 브릿지로 붙어 있다.

## 절대 규칙

1. **코드를 고치기 전에 사용자와 합의한다.** 조사·분석·설명은 자유롭게 하되,
   파일을 바꾸는 건 "그렇게 하자"는 말을 들은 뒤에.
2. **`config.json` 에 API 키가 평문으로 들어 있다.** 동기화 폴더·배포본·문서에 넣지 마라.
3. **목록·개수를 외워서 말하지 마라.** 톤이 몇 종인지, 카메라가 몇 종인지는 자주 바뀐다.
   `docs/_현황.md` 를 읽거나 `main.py` 를 임포트해서 세라.
4. **실측 결과를 추측으로 덮지 마라.** 코드 주석에 `(YYYY-MM-DD 실측: …)` 이 91곳 있다.
   돈을 써서 얻은 값이라 다시 얻기 어렵다. 반대되는 주장을 하려면 다시 측정해서 근거를 대라.
5. **작업을 마치면 문서도 같이 닫는다.** 실측이 나왔으면 `docs/05` 에 절 추가, 도구·톤이
   바뀌었으면 `/문서갱신`(자동생성 2종 재생성), 쓰는 법이 바뀌었으면 `docs/시트_사용법`
   과 앱의 사용법 페이지. 문서에 안 남긴 작업은 다음 세션이 모른다.

## 전체 흐름

```
대본  →  컷 분해 JSON  →  이미지 프롬프트(약 3,500자)  →  2K 이미지  →  I2V 영상  →  편집
       ↑ claude.ai        ↑ 앱이 조립                    ↑ Seedream/Gemini  ↑ Seedance/Veo
```

**컷 분해만 앱 밖(claude.ai)에서 돈다.** 나머지는 전부 앱 안이다.
그래서 **지침을 고치면 앱 빌드로는 반영되지 않는다** — 사용자가 `[📋 지침 복사]` 로 뽑아
claude.ai 프로젝트 지식을 직접 교체해야 한다. 지침 첫 줄의 8자리 번호로 최신 여부를 대조한다.

## 설계 원칙 — 이걸 모르면 엉뚱한 데를 고친다

**LLM 은 짧은 부품 값만 채운다. 조립 문법은 앱이 소유한다.**
컷 JSON 24개 필드는 전부 짧은 값(`focus_en`, `motion_en`, `style`, `reveal` …)이고,
질감·색·카메라·강조·네거티브·얼굴 정책은 앱이 톤과 상황을 보고 붙인다.
LLM 이 완성된 영어 프롬프트를 쓰게 하면 톤이 컷마다 흩어진다.

| 어디서 고치나 | 무엇을 |
|---|---|
| **지침** (claude.ai) | 컷 경계 · beat · style 선택 · reveal 선택 · chain · 각 필드에 뭘 쓸지 · 예산 규칙 |
| **코드** (`main.py`) | 프롬프트 조립 · 게이팅 · 상한 · 카메라 결정 · 클립 길이 · 네거티브 |

## 어디를 봐야 하나

| 알고 싶은 것 | 파일 |
|---|---|
| 지금 톤·카메라·리빌·모델·단가가 뭐가 있나 | **`docs/_현황.md`** ⚙자동생성 |
| 전체 흐름, 어느 단계에서 뭐가 결정되나 | `docs/01_전체흐름.md` |
| 이미지 프롬프트가 어떻게 조립되나 | `docs/02_이미지생성탭.md` |
| **영상화 두 갈래가 왜 다른가** (제일 헷갈림) | `docs/03_영상화_두갈래.md` |
| 지침과 코드 중 어디를 고칠까 | `docs/04_지침과코드_경계.md` |
| 실험해서 알아낸 것 (돈 쓴 것) | `docs/05_실측기록.md` |
| 강조 도구 총람 — 필요 필드·영상화·조립 | **`docs/06_강조도구.md`** ⚙자동생성 |
| 컷 JSON 24필드 한글 설명 | `docs/컷JSON_필드_한글설명.md` |
| 시트 3종(톤·캐릭터·피사체) 쓰는 법 | `docs/시트_사용법.md` |
| 실험 증거 이미지·영상 원본 | `_실험/` (색인은 `docs/05` 맨 끝) |
| claude.ai 에 올릴 지침 사본 | `지침/컷분해지침_<해시>.md` — 최신 하나만 둔다. 옛판은 `지침/_옛판/` |
| 지난 대화 산출물 | `docs/_아카이브/` (시점 기록 — 지금 코드와 다를 수 있음) |

## 자주 밟는 함정

- **영상화 경로가 둘이고 거의 공유하지 않는다.** 컷 영상화(`_build_motion_prompt`)와
  자료 소스 탭(`source_video_prompt`)은 `_gen_seedance`/`_gen_veo` 에서만 만난다.
  한쪽을 고치고 "둘 다 됐다"고 말하면 틀린다. → `docs/03`
- **엔진마다 쓸 수 있는 초가 다르다.** 베오 표를 시댄스에 적용하면 10·12초가 4초로 눌린다.
- **I2V 에서 `subject_en` 은 영상 프롬프트에 안 들어간다.** `motion_en` 만 자유 문장이다.
- **강조가 붙은 컷은 카메라가 강등된다** (`CAMERA_LOUD`). 큰 워크를 골라도 안 먹는다.
- **톤 레퍼런스 / 캐릭터 시트 / 피사체 시트는 전역 설정이다.** 한 번 걸면 지울 때까지
  모든 컷·모든 편에 붙는다. 장면 사진을 피사체 시트로 걸면 엉뚱한 컷에 그 장면이 재현된다.
- **인물이 셋 이상이거나 작게 잡히면 영상에서 무너진다.** 인원수·배치가 프레임마다 바뀐다.
  → `docs/05` (2026-08-15 실측)

## 빌드

```powershell
.\배포본_만들기.ps1 -Force     # 재빌드 + 배포. 앱이 켜져 있으면 실패한다
```
파이썬 **3.13** 으로 빌드해야 한다 (`python` 명령은 3.10 을 가리킨다). 스크립트가 알아서
`_internal` 의 `pythonNNN.dll` 을 읽어 맞는 판을 고르므로 직접 `pyinstaller` 를 부르지 마라.
빌드 직전에 `docs/_현황_뽑기.py` 가 돌아 `docs/_현황.md` 를 갱신한다.

**프롬프트 문구를 고쳤으면 빌드 전에 `tests\전체검사.ps1`** (컴파일 + 파이썬 회귀 + UI 정합성).
강조 도구를 만들거나 고쳤으면 `python docs/_강조도구_뽑기.py` 로 `docs/06` 을 다시 뽑는다.

반복 절차는 스킬로 있다: `/재빌드` · `/지침교체` · `/문서갱신` (`.claude/skills/`)

## 배포 (2026-08-19 체계)

- 소스: `github.com/edithtech-lab/youtube` — push 는 원격 URL 에 계정(edithtech-lab)이
  박혀 있어 무인 동작한다 (이 PC 에 GitHub 계정이 3개라 미지정 push 는 무한 대기).
- 실행본: GitHub **Releases** — 받는 쪽은 `releases/latest` 에서 zip. 릴리스 업로드는
  GCM 토큰 재사용 스크립트로 자동화돼 있다 (`config.json` 은 zip 에서 원천 제외).
