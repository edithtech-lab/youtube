# 실측 기록 — 돈과 시간을 써서 알아낸 것

**추측으로 덮지 마라.** 여기 있는 건 전부 실제로 이미지·영상을 뽑아보거나 API 를 때려보고
얻은 값이다. 다시 얻으려면 또 돈이 든다. 반대되는 주장을 하려면 **다시 측정해서** 근거를 대라.

> 원본은 `main.py` 코드 주석이다 (`(YYYY-MM-DD 실측: …)` 형태로 51곳).
> 이 문서는 **결정에 영향을 주는 것만** 골라 주제별로 묶은 것이다.
> 코드를 고칠 때는 그 자리의 주석을 읽어라 — 문맥이 거기 있다.
>
> 다시 뽑기: `grep -n "실측\|검증\|자문" main.py`

---

## 인물 — 몇 명까지 버티나 (2026-08-15)

`whitecast` 톤, 4초, 720p, 오빗, 세 장을 뽑아 0.5초 간격으로 프레임을 갈랐다. (약 909원)

| 배치 | 결과 |
|---|---|
| **2명 · 크게** | **완전 유지.** 자세·인원 그대로, 4초 내내 **얼굴이 안 생겼다** (`VIDEO_CAST_LOOK` 실증) |
| **4명 · 중간 거리** | 수는 유지되나 **형태가 흐물거림**. 시키지 않은 연기가 생김 |
| **8명 · 부감** | **인물이 사라졌다 나타남.** 배치가 프레임마다 재구성. 건물까지 새로 생성 |

**결론** — 인물이 셋 이상이거나 작게 잡히면 아무도 움직이지 마라. 부감·와이드로 여럿을
보여주는 컷은 카메라도 고정하라. **크게 도는 오빗이 붕괴의 방아쇠였다.**
움직여야 하면 `slowpush`·`slowslide` 만.

> 옛 주석의 "안개"는 **기법이 아니라 실패 증상**이다 (모델이 없던 안개를 지어냄).
> 다만 **이미지 단계에서** 의도적으로 뒤집어 쓸 수는 있다 — `subject_en` 에
> "뒤쪽은 옅은 안개에 잠겨 실루엣만" 을 적으면 일관성 흔들림을 가릴 수 있다.

**일관성 유지 우선순위**: ① 인물은 크게·적게 ② 피사체 시트 ③ 캐논 문구 ④ 카메라 묶기 ⑤ 뒤쪽 흐리기

---

## whitecast·greycast 발광 (2026-08-15)

레퍼런스 20.6분(`레퍼런스_D8nt3dt8vT4/`)을 4초 간격 309프레임으로 갈라 측정했다.

| | 측정값 |
|---|---|
| 컷 | 약 181컷 · 평균 6.9초 / **중앙값 5.5초** · 분당 8.8컷 |
| 세계 밝기 | 평균 **100/255** — 밝은 흰 세계가 아니다 |
| 검정 배경 컷 | **17%** · 밝은(설원) 컷 13% |
| 세계 색조 | 어두운 채도색 85% 컷, 그중 시안·파랑 90% → 차가운 청록회색 |

**측정 함정 둘** — 같은 실수를 반복하지 마라:
- "발광 34%"는 **흙 단면의 갈색 질감**을 발광으로 잘못 셌다
- 첫 채도 측정은 **인물의 남색 의상**을 강조색으로 잘못 셌다
- → 발광만 재려면 **밝기 150 초과 + 채도 35% 초과**로 걸러야 한다

**톤 문구가 발광을 스스로 막고 있었다.** `No colour anywhere except the neutral scale…` 와
강조 블록의 `A bright cyan (#22D3EE)…` 가 한 프롬프트 안에서 충돌했다. 모델은 이럴 때
절충해서 흐린 회분홍을 그리거나 강조를 지운다 — 컷마다 결과가 달랐던 원인이다.

**고친 뒤 실측** (2K 샘플 6장):

| 컷 | 조건 없이 열었을 때 | 조건화 후 |
|---|---|---|
| `whitecast` 강조 없음 | 채도 **19.30%** (감시탑이 제멋대로 빛남) | **0.00%** |
| `greycast` 강조 없음 | 3.74% | **0.00%** |
| `extent` 강조 있음 | 13.77% | 9.62% (유지) |

색 예외는 **"강조 지시가 있을 때만, 그 색으로만"** 이라고 조여야 한다. 그냥 열면
모델이 발광 여부와 색을 스스로 정해 컷마다 달라진다.

**`almost shadowless` 는 흰 모형의 적이었다.** 그림자가 형태를 읽히게 하는 유일한 수단인데
그걸 지우고 있었다. 낮은 각도 방향광 + 긴 그림자로 바꾸자 면 구분이 살아났다.

**`extent` 신설 근거** — 레퍼런스의 울타리 컷은 `glow`(덩어리 하나)·`count`(점)·`zone`(평면
영역) 어느 것으로도 안 된다. 선형으로 뻗은 것이 **얼마나 멀리 가는지**가 요점이라 별도 도구다.

---

## 조립 강조 CLEAN→INFO (2026-08-18)

레퍼런스 두 편(피렌체 돔·티거 88mm)의 "그래픽이 나레이션에 맞춰 그려지는" 효과를 역추적해
도입했다. 정체는 **시작+도착 프레임 보간**: CLEAN(강조 없음)에서 출발해 INFO(강조 구움)를
도착 프레임으로 걸면 그래픽이 클립 안에서 조립된다.

| 실험 | 결과 |
|---|---|
| draw (그리라고만 지시, 목표 없음) | 2.5초까지 빈 화면 — **기각** (2026-08-16) |
| Seedance 4초 + INFO 도착 | 4단계 조립 완주, 끝 상태 고정 ✅ |
| Veo 4·6초 + 도착 프레임 | **400 통째 거부** — 보간은 8초 전용 |
| Veo 8초 + INFO 도착 | 조립 완주, 선이 가장 얇고 정밀 ✅ (lite 도 됨) |
| Veo 말 지시만 (목표 없음) | 4단계 중 2단계에서 멈춤 |
| Seedance 8초 동일 프롬프트 | 조립되나 **중간 도형 발명** (UFO 같은 이중 고리) |

**어휘 함정**: 프롬프트의 `anchor dot` 을 Seedance 가 문자 그대로 **⚓ 닻 아이콘**으로
그렸다 (같은 문구를 Veo 는 점으로 해석). → 조립 문구는 `small glowing dot` 로 쓰고
"다른 도형·아이콘·심벌 발명 금지"를 명시한다 (`VIDEO_ASSEMBLE_TMPL`).

**기존 방식의 3초 소실**(화살표가 4초 클립에서 3초에 사라짐, slowpush 로도 못 막음)이
조립 방식에선 구조적으로 불가능하다 — 도착 프레임에 그래픽이 있으므로.

**INFO 편집 패스**: CLEAN 을 참조로 "강조만 얹어라" 편집이 구도를 보존했다 (`INFO_EDIT_HEAD/TAIL`).
graph 도구(축+눈금+곡선+워시)는 이 실측에서 문구가 검증돼 21번째 도구로 등록됐다.

## 신도구 4종 시제품 (2026-08-18)

주제(제품·무기·인체·건축·인물사건) × 도구 21종 갭 분석에서 나온 4종을 시제품으로 검증하고
등록했다 (강조 25종). **4종 모두 첫 시도에 성립** — 문구를 그대로 등록했다.

| 도구 | 시제품 장면 | 결과 |
|---|---|---|
| `wave` | 야포 포구 충격파 | 동심 링이 지면 따라 타원으로 기울며 퍼짐 ✅ |
| `skeleton` | 인체 혈관망 | 몸 **속에서** 빛나고 팔을 따라 굽음, 원근·가림 유지 ✅ |
| `loadsplit` | 거석 문 하중 우회 | Y 분기 → 양쪽 벽 → 지면 화살촉, **가운데 깨끗** ✅ |
| `trajectory` | 곡사포 탄도 | 발사점 점 → 포물선 → 착탄 화살촉+지면 링 ✅ |

원본: `_실험/신도구/`. 문구 전부 `anchor` 단어 미사용(⚓ 함정 회피, `small glowing dot`).
`flow_of` 에 `blood`(진홍 #B91C1C) 추가 — 인체 주제용, force 의 발광 적색과 구분.

## 카메라 — 강조가 붙으면 못 움직인다 (2026-08-12)

- **6초 클립에서 화살표가 3초 만에 프레임을 벗어나 사라졌다.** 문구로는 못 막는다 →
  코드에서 `CAMERA_LOUD` 강등으로 처리
- **레퍼런스도 계측 그래픽이 있는 컷은 카메라가 거의 고정**이었다
- 치수선 굵기: 영상은 720p 로 나간다. 1080p 기준 1~2px 을 요구하면 실효 1px 미만이 되어
  **렌더 중에 선이 사라진다.** 절대 픽셀 대신 "축소 후에도 남는 굵기"로 지시한다
- 카메라를 `slowpush` 로 묶어놓고 템포가 "빠른 리프레임 환영"이면 **두 지시가 정면 충돌** →
  `quiet` 면 템포도 `calm` 으로 강제

## 자료 화면 톤은 카메라가 움직이면 무너진다 (2026-08-13)

**미니어처 설원 컷을 느린 돌리로 뽑았더니 스키어가 5명 → 2명 → 7명이 되고 없던 안개가 생겼다.**

실사 장면은 카메라가 움직여 새 영역이 드러나도 모델이 그럴듯하게 채운다(나무가 몇 그루든
아무도 모른다). **자료 화면은 구조가 명확해서 하나만 달라져도 바로 티가 난다.**
→ `_auto_camera` 가 `SOURCE_STYLES` 의 카메라를 묶는다. `tabletop` 만 예외(시차가 있어야 모형으로 읽힘).

---

## 글자 — 영상에 새기면 뭉갠다 (2026-08-06)

- **720p·6초 클립: "20km" → "2?:00"**. 숫자는 2K 이미지에 굽거나 편집 자막으로 얹는다
- **한글 라벨은 AI 가 뭉갠다: "등대 높이" → "롱대 늪이".** 숫자·영문 단위는 깨끗하다
  → 라벨 모드는 '숫자 치수만' 허용, 한글은 편집에서
- 영상은 **유지만** 시킨다 (`VIDEO_TEXT_KEEP`). 새로 그리게 하면 720p 에서 뭉개진다

## 강조 — 끝점이 없으면 축을 찍는다 (2026-08-12)

- **대회랑 '높이'를 물었는데 세 번 중 한 번은 통로 '폭'에 43m 를 붙였다.**
  하중 화살표는 좌우로 갈라지는 핵심을 못 그려 **설명이 뒤집혔다**
  → `measure`·`arrow`·`flow`·`route` 는 `from_en`/`to_en` 둘 다 필수
- 첫 문장만 `holographic HUD` → `technical dimension line` 으로 바꾸자 **세 번 다 바로잡혔다**
- 코너를 못박지 않으면 **한 라벨을 두 번 그린다** (ACTUAL 이 위·아래에 각각 찍혀 라벨이 셋)
- **가리킬 대상(`focus_en`)이 없으면 주석을 아예 넣지 않는다** — 지시가 없으면 AI 가 화면을
  장식으로 채워 맥락 없는 화살표가 흩뿌려진다 (2026-08-06)

## 톤이 피사체를 덮어쓴다 (2026-08-04)

**`arch3d` 톤이 모든 컷을 댐으로 만들었다.** → 톤 직후에 `SUBJECT_LOCK` 을 못박는다.

> 전역 톤 지정 자체는 정상이다 — 5배치 100% 균일 확인. 룩이 섞이면 원인은 대개
> `subject_en` 의 매체 어휘(`cinematic`, `black-and-white`, `drone shot`)다. 지침 `[1]-8` 이 금지.

---

## API 문서와 실제가 다른 곳

| 항목 | 문서 | 실제 |
|---|---|---|
| Veo 초 (2026-08-04) | "between 4 and 8, inclusive" | **5초·7초도 거부.** 짝수 3개만 (4·6·8) |
| Veo 해상도 | — | 720p 외에는 **8초 고정** → 비용 2배. lite 는 4k 미지원 |
| Veo 이미지 키 | `image.inlineData` | **400.** `bytesBase64Encoded` 만 수락 |
| Veo `durationSeconds` | 문서 예시가 `"4"` 문자열 | **숫자여야 한다.** 문자열은 400 |
| Veo `numberOfVideos` | — | Veo 3.1 이 안 받는다. 보내면 400 |
| Veo 도착 프레임 (2026-08-10) | — | `instances[0].lastFrame` (SDK 직렬화 코드로 확정) |
| Seedance 초 | — | 2~12초 자유. 해상도 무관 |
| Gemini 이미지 (2026-08-03) | PNG | **lite 는 JPEG 로 온다.** 매직넘버로 판정해야 함 |
| Seedream 크기 | — | 최소 약 369만 픽셀. 작게 못 뽑음 |
| Seedream 단가 (2026-08-06) | — | **장당 과금 — 해상도 무관** |
| ModelArk 잔량 (2026-08-11) | — | `PageSize` 안 주면 10개만 옴. 값 있는 건 뒤쪽 (42개 중 4개) |

**⚠ 재발한 버그**: 해상도 제한은 **Veo 전용**이다. 엔진을 안 보면 Seedance 의 5·10·12초가
4초로 떨어진다. 2026-08-13 수정이 반쪽이라 재발했고 **2026-08-14 에 다시 잡았다**
(10초를 골라도 4초 클립이 나왔다).

---

## 낭독·자막 (2026-08-08 · 08-11)

- **낭독 속도 8.7자/초** — 컷 수 계산과 클립 길이 배정의 기준
- 같은 문장 안 단어 간격 **중앙값 0.04초**, 문장 경계 **0.2~0.8초** → `0.25` 를 경계로
- 음슴체 대본은 마침표가 없어 부호로 문장을 못 찾는다. **쉼으로 찾는다**
- 단어 타임스탬프만 보면 못 찾는다 — 44초 음성에서 250ms 넘는 텀 **0곳**.
  파형(dB)은 같은 파일에서 **8곳 1.4초**를 잡아냈다
- 의존명사 붙이기를 `startswith` 로 판단하면 "원래"·"만든"·"데이터"까지 붙는다

## 체인 (2026-08-06)

LLM 이 "다 이어라"로 폭주하면 **장면 전환이 사라진다.** 고정 상한 대신 **대본이 실제로
같은 공간에 머무는지**로 판단한다 — `place_en` 이나 피사체가 바뀌면 끊고, 머무르면 길게 허용.

## 강조 개수 (2026-08-14)

상한을 걸면 **컷 번호가 큰 쪽부터 꺼져 후반의 회수 컷이 먼저 죽는다.**
「모아이」 17컷 실측에서 Opus 가 정확히 4컷만 켰으므로 **개수는 분해기 판단에 맡긴다**
(`anno_max_cuts` 기본 0 = 무제한).

## 레퍼런스 채널 분석

| 채널/영상 | 측정 |
|---|---|
| 강조 도구 (2026-08-11) | 47컷 실측 → 강조로 **문장의 논리를 그린다**. "물이 차오릅니다"→상승 화살표, "그쪽은 안 됩니다"→X |
| 냉각탑편 | 36컷 / 컷당 평균 **3.1초** |
| Cipher (25분) | 241컷 / 중앙값 **4.1초** |
| IMPERIALYT (44분) | 50% 3D 디오라마 + 38% 그린 터미널 UI. **UI 는 AI 재현 불가** (글자가 정보이고 레이아웃이 반복돼야 함) |
| 인물 이동 (2026-08-15) | 8컷 전수 — **인물이 실제로 이동하는 컷은 25%뿐.** 나머지는 카메라가 물러나거나 훑거나, 화면의 수치·치수선이 자라났다 |

**→ 화면이 변해야 하는데 인물이 못 움직이면, 카메라와 그래픽이 변화를 맡는다. 그게 정상이다.**

## 톤 신설 근거 (2026-08-15)

- **`greycast`** — 얼굴 없는 회색 마네킹. **실존 인물 문제를 근본적으로 푼다.**
  `game`·`story3d`·`toy3d` 는 얼굴을 그리므로 "실제 인물을 닮게 그리지 마라"는 금지 규칙에
  기대야 하지만, 이 톤은 얼굴 자체가 없다
- **`whitecast`** — greycast 의 밝은 짝. 갈리는 지점 셋: ① 어두운 무대가 아니라 밝은 흰 세계
  ② 옷이 암시가 아니라 **실제 형태**(전술조끼·패딩·정모) ③ 총구 연기·파편 허용
- **`blackstage`** — ① 순수 검정 배경 ② 흰 무광 캐스트 ③ 시안 발광 곡선 하나

---

## 환경 (2026-08-13)

- **QtWebEngine 은 클립보드가 기본 꺼짐.** `execCommand('copy')` 가 조용히 `false` 만 돌려주고
  오류도 안 난다 (자막·낭독본·지침 복사가 전부 무반응이었다).
  `navigator.clipboard` 는 `file://` 이 보안 컨텍스트가 아니라 아예 못 쓴다
- claude.ai 분해 실패 사유를 안 남기면 **크레딧이 떨어져 몇 주째 Gemini 로 돌고 있어도 모른다**

---

## 증거물 위치 (실험 이미지·영상 원본)

돈 주고 뽑은 실측 증거는 전부 `_실험/` 아래에 있다 (2026-08-18 통합).

| 폴더 | 무슨 실험 | 관련 절 |
|---|---|---|
| `_실험/강조카탈로그/` | 도구 예시 26장 + 종합시트 + 개선4종 전후비교 | 신도구·조립 |
| `_실험/신도구/` | wave·skeleton·loadsplit·trajectory·spin 시제품 | 신도구 4종 |
| `_실험/조립형/` | CLEAN→INFO 조립 (시댄스 vs 베오, 8초 보간) | 조립 강조 |
| `_실험/draw/` | 구운 그래픽 vs 영상이 그리기 비교 | 글자·강조 |
| `_실험/인원수/` | 인원수별(2·4·8명) 영상 붕괴 | 인원 함정 |
| `_실험/샘플검증/` | versus·extent·캐스트 문구 반복 검증 | 각 도구 절 |

1편 산출물: `D:\캡컷\소스\20260818_스톤헨지는 두 번째로 서 있다\` (검수 사본 `_1편_스톤헨지/`)

## 조립 전이 문법 확정 여정 (2026-08-19)

같은 날 4차례 실측으로 문법을 확정했다. **시댄스의 전이 구간 발명은 문구로 줄일 수는
있어도 소거는 안 된다** — 3회 반복 측정된 엔진 특성이다.

| 차수 | 문법 | 결과 |
|---|---|---|
| 1 | 성장형("부품이 순서대로 그려진다") | 2편 #9 ㄱ자·가로팔 발명, #7 화살표 6초 요동 |
| 2 | 정착형(동시 페이드인) | 실사 컷에서 차체 윤곽·포신 잔선 낙서 (측정선 자체는 안정) |
| 3 | 그리기형("보조선→선→수치") | 회색 단면에선 성공, 초반 화면 밖 진입 방황 |
| 4 | **제자리 발생**("완성선이 그 자리에서 떠오름, 픽셀 고정") | ✅ 확정 — 이동 경로가 없으니 방황할 곳이 없다 |

- **배경이 단순할수록 낙서가 준다** — 무채 정적 배경(회색 단면)은 낙서 소재 자체가 없다.
  조립 강조는 도면·단면·제품 컷에서 진가.
- count 는 순차 점등이 정답 (동시 페이드인은 과교정 — 순차 자체는 2편 #8 에서 성공했었다).
- 조립 템포: "1/3 지점까지 완료 후 정지" 지시로 수치 등장 4.2→2.8초. 더 조이는 건 문구 한계.

## 씬2 사건 — 시간 초과의 진범 (2026-08-19)

크록스 편 #2(1080p+조립+효과음)만 반복 "시간 초과(10분)". 재현 실측:
- **같은 조합을 이 컴에서 돌리니 148~229초에 성공** — 컷 내용·조합 무게는 무죄
- 진범 후보: 코드의 **10분 고정 대기**(해상도·조립 무관) + 계정별 서버 큐 정체
- 처방: 조립·1080p 는 25분 대기 + 시간 초과 메시지에 마지막 서버 상태 표시

## Seedance·Veo 단가 공식 대조 (2026-08-19)

- Seedance 토큰식 **(W×H×24×초)÷1024** 공식 확인, 무음 $1.2/M·유음 $2.4/M — 앱 표와 일치
  (720p 6초 실측 130,500t · 1080p 293,625t = 공식값 ±1%)
- **Veo fast 1080p 는 $0.12** — 앱에 0.10 으로 잘못 있어 정정 (예상가가 17% 싸게 보였다)
- Veo 는 오디오 포함 단가, Seedance 는 효과음 켜면 정확히 2배

## 무료 잔량 — 로컬 추정의 한계와 실측 동기화 (2026-08-19)

컴퓨터가 2대가 되면서 로컬 추정(이 앱이 쓴 양만 집계)이 낙관적으로 어긋나는 구멍 확인.
→ 배치 시작 직전 ListModelChargeItems 실측 1회(5분 캐시)로 잔량 0 계정을 (소진) 제외.
⚠ 이 API 는 몇 분 지연이 있어 **배치 중 재조회 금지** — 기준점+로컬 차감 구조 유지.
GitHub push 무한 대기의 진범은 저장된 계정 3개 → 원격 URL 에 계정 고정으로 해결.

## 영상 판 파일명 전환 — 캡컷 소재 보호 (2026-08-19)

**문제**: 재생성이 같은 이름 자리를 갈아치워, 캡컷에서 편집 중인 소재가 조용히 다른
영상으로 바뀌었다 (캡컷은 경로+파일명으로 소재를 문다).

**전환**: 영상은 `NN_장면_v1.mp4 / _v2 / _v3` 로 **옆에 쌓는다** — 기존 파일을 옮기지도
덮지도 않는다. 앱은 최신 판을 보여주고, 컷 카드 📼 칩으로 판을 오간다(보기 전용).

- 실전 검증: 판수 2 배치 → 재생성 1회 = v1·v2·v3, **v1 md5 불변**, v폴더 미생성
- 정수 정렬 필수 (`_v10 < _v2` 함정) · 묶음 기준은 **컷 번호**(장면명은 바뀔 수 있다)
- 번호 없는 옛 파일은 v0(레거시)로 취급 — 기존 편과 섞여도 안전
- **이미지는 현행(v 폴더) 유지** — 앱이 이미지 경로를 실제로 읽는다(체인·조립 CLEAN 짝).
  영상은 사실상 쓰기 전용이라 전환 비용이 낮았다.

## 강조 재질 이원화 A/B — 매트가 이긴다 (2026-08-19)

레퍼런스 4편 분석(빨강·매트 문법)을 받아 arrow·reject·route 를 "유리·발광"에서
"매트·불투명"으로 바꾸고 같은 장면으로 A/B 생성 (7장 357원, `_실험/재질AB/`).

| 도구 | A 기존 (유리·시안) | B 개선 (매트·의미별색) |
|---|---|---|
| arrow | 거대 유리 화살표가 포신 전체를 덮음 + **촉이 양쪽** (07 컷 오염과 동일 현상 재현) | 가는 매트 빨강, 촉 한쪽, 대상 안 가림 |
| reject | 화면 중앙 네온 튜브 X — SF 로고 느낌 | 평면 빨강 스탬프 — 레퍼런스 문법 그대로 |
| route | 굵은 광택 리본이 공중에 뜸 | 바닥에 붙은 가는 선 + 시작/끝 점 |
| ring(신규) | — | 나무 상자 무리에서 한 상자만 정확히 지목, 원근 타원화 |

- **"촉은 한쪽 끝에만" 문구가 실제로 먹혔다** — A 에서는 지시 없이 양촉이 나왔다.
  양방향 오염은 문구 부재가 원인이었음이 재확인됨.
- 재질 지시(matte/opaque/no glow)는 Seedream 이 충실히 따른다 — 렌더러가 아니라도
  재질 축 분리가 성립한다.
- 색: hybrid(의미별) 신설 — 도형=red, 물리현상=cyan, 도면 톤=white 유지, flow_of 우선.
  UI 강조색에 '의미별' 선택지 추가. 이때 ring 포함 26종.

## ring·reject 조립 실측 2회 실패 → 조립 제외 (2026-08-20)

- 1차(공용 문구): ring 거대 링→축소 + 해치 테두리 발명 / reject 화면 위에서 낙하 착지
- 2차(도구명 박은 제자리 스탬프 전용 문구): ring 점→성장 + 이중 링 발명 + 발광화 /
  reject 획 회전 조립 + 물감 튀김 발명
- **결론: 팝 계열은 조립 자체가 위험하다** — measure(도면 세트 페이드인)는 되는데
  단순 도형은 시댄스가 등장 과정을 "발명"한다. `ASSEMBLE_SKIP` 에 ring·reject 추가,
  같은 원리로 ghost·labelcallout 도 실측 없이 태우지 않음 (2026-08-20 사용자 결정).
- 영상 원본: `_실험/조립3종/` (v1 공용 / v2 전용문구 비교 가능)

## 레퍼런스 치수선 등장 방식 3종 (2026-08-20 프레임 판독)

① 드로우+숫자 카운트업 (군함 15.2m 6.4→7.3s, 성벽 46km) — 우리가 4차 실측에서 기각한
   그 방식. 레퍼런스는 AE 편집이라 가능, I2V 는 모핑 위험.
② 완성 세트 페이드인 (다락 글라이더 98.9→100.3s) — **우리 제자리 발생과 동일.**
   같은 샷 안에서 페이드아웃까지 함 (우리는 편집 컷으로 동일 효과).
③ 순차 축적 (성벽 BLOCK12 83→86s) — 보조 치수가 하나씩 쌓임. 우리 도면형 보조선의
   애니판. 필요 시 조립 2컷(성긴→밀한)으로 실측 검증 예정.
→ measure 현행 유지 판정.

## 신도구 4종 + 지침 대개정 (2026-08-20)

- field(지속 탐지 돔)·ghost(잔상)·solidbar(3D 막대)·labelcallout(라벨 태그) 정식 추가
  — 30종. 예시: `_실험/2차후보4종/`. labelcallout 은 라벨 없으면 ring 강등,
  영상 draw 판 없음(글자 발명 금지).
- 지침 0e881d45: 강조 상한 폐지(질적 규칙 3개로 대체 — 같은 도구 3연속 금지·대형 1~2회·
  분위기 컷 비움) + ring 등 5종 노출 + shot=screen 도해 금지 + 신톤 2종 선택 기준.

## 재질 확장 2차 — 사용자 판정: 대부분 현재 유지 (2026-08-20)

같은 댐 소재로 5종 A/B + glow 모드 3종 시안 (357원+51원, `_실험/재질B_2차/`).
- graph·gauge·extent·marker: 매트 B안이 기술적으론 성립했으나 **사용자 판정 현재(시안
  발광) 유지** — 채널 정체성. B안 폐기.
- **outline 만 개정**: 또렷한 코어 + **바깥쪽으로만 번지는 발광** + 내부 무채색 유지
  (outline_v2.jpg — 게이트 격자가 그대로 보이면서 지목 명확).
- **glow 범위 자동화**: "전체 점등" 강제 문구 → **focus_en 이 말하는 범위만** 점등
  (전체/부품/표면 얼룩이 focus 표현으로 갈림 — part·surface 시안으로 검증).
  모드 필드는 안 만들었다 (LLM 은 짧은 값만 원칙).
- 지침 824eac49: glow 선택 기준 갱신 (0e881d45 는 교체 전이라 폐기, 신판 직행).
